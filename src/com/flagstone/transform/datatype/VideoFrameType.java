package com.flagstone.transform.datatype;

/** TODO(class). */
public enum VideoFrameType {
    /** TODO(doc). */
    KEY,
    /** TODO(doc). */
    NORMAL,
    /** TODO(doc). */
    OPTIONAL
}
