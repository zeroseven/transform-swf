package com.flagstone.transform.util.image;

/** TODO(class). */
public enum ImageFormat {
    /** TODO(method). */
    IDX8,
    /** TODO(method). */
    IDXA,
    /** TODO(method). */
    RGB5,
    /** TODO(method). */
    RGB8,
    /** TODO(method). */
    RGBA;
}
