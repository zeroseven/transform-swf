package com.flagstone.transform.button;

/**
 * ButtonType is used to identify whether a button will be rendered as a 
 * pull-down menu or push button. 
 */
public enum ButtonType {
    /** The button is used to create pull-down menus. */
    MENU,
    /** The button is used to create a push button. */
    PUSH;
}
