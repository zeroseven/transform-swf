package com.flagstone.transform.fillstyle;

/**
 * GradientType is used to identify whether a gradient file is linear or 
 * radial. 
 */
public enum GradientType {
    /** The gradient is rendered linearly. */
    LINEAR,
    /** The gradient is rendered radially. */
    RADIAL;
}
